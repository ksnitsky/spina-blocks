<%= render Spina::MainNavigation::LinkComponent.new(
  t('spina.blocks.title', default: 'Blocks'),
  spina.admin_blocks_path,
  active: controller_name.in?(%w(blocks block_categories page_blocks))
) %>
